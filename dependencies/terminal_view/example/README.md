# terminal_view demo

A single-screen Flutter app that wires `TerminalView` to a toy shell written in
Dart, so it runs on any platform with no PTY or SSH setup.

```bash
flutter run
```

Type `help` in the app for the commands it knows.
